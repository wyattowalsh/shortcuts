# Network Note

Fetches a note from `https://example.com/note.txt` and writes it to the clipboard.

This fixture intentionally declares network access so catalog and security docs can show data-egress trust signals.
