# Tasks

- [x] 1. Add OpenSpec deltas for CLI, manifest, security, catalog/docs, and release behavior.
- [x] 2. Implement HTML analyzer and bundler helpers with deterministic JSON models and fixtures.
- [x] 3. Add `shortcutkit html analyze`, `bundle`, and here.now `publish` commands with `--dry-run` default and explicit `--run` opt-in.
- [x] 4. Extend manifest schema/models for `html_runtime` and update Open HTML metadata.
- [x] 5. Update security scanning for HTML/CSS/JS source and runtime profile risks.
- [x] 6. Update catalog/docs/release rendering for HTML runtime metadata and generated bundle assets.
- [x] 7. Replace Open HTML file-link Cherri source with bundled-only `data:` URL flow.
- [x] 8. Add and run targeted tests, package validation, strict audit, catalog/docs checks, and OpenSpec validation.
- [x] 9. Write implementation handoff with verification, blockers, and device-test limitations.
